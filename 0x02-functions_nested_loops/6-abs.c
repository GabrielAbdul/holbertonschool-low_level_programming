#include "holberton.h"

/**
 * _abs(int) - function that computes the absolute value of and integer
 * @int: integer input by user
 * Return: (void)
 */

int _abs(int)
{
	if (int >= 0)
	{
		return (int);
	}
	else
	{
		return (int * -1);
	}
}
